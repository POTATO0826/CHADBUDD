{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "tgweb",
  "description": "The Telegram window may invoke app commands — needed so the injected call script can report its progress back to the terminal. Scoped to the one window and the one origin; the exposure is the app's own command set, accepted deliberately: the alternative was debugging a remote page blind, which the white-screen hunt proved impossible.",
  "windows": ["tgweb"],
  "remote": {
    "urls": ["https://web.telegram.org", "https://web.telegram.org/*"]
  },
  "permissions": ["core:default"]
}
