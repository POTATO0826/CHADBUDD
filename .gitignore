# dependencies
node_modules

# output
dist
out
*.tgz

# env
.env
.env.local
.env.*.local

# caches
.cache
*.tsbuildinfo

# editors
.idea
.DS_Store

# rust / tauri build output (added later)
src-tauri/target
src-tauri/gen/schemas

# debug screenshots
_shot*.png

# message bridges. Both of these are FULL account sessions in plaintext —
# anyone holding them is logged in as you. Never commit either.
.wa/
.tg/

# throwaway backend probes
scripts-local/

# agent tooling, installed per-machine by the convex plugin
.agents/
.claude/
skills-lock.json
convex/_generated/ai/

# local database exports — real client conversations, never commit
backups/

# python sidecar (bridge/emotion) — uv manages the environment out of tree
__pycache__/
